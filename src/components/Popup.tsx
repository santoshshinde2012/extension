import './Popup.css';

const Popup = () => {
  return (
    <div className="popup-container">
      <h1 className="popup-title">Chrome Extension!</h1>
      <p>This is a popup created using React.js, TypeScript, and Vite.</p>
    </div>
  );
};

export default Popup;